{
  "name": "@wpsync/core",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Sync library for wpsync — WordPress REST client, front-matter codec, pull/push engines, conflict detector.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/iarna__toml": "^2.0.5"
  }
}
